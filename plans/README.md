# plans/

Working planning notes. `UNFINISHED.md` here is **machine-written** by
`.claude/hooks/workflow_hook.py` at the end of a session with a dirty tree (a "resume here"
breadcrumb) and is gitignored — never commit it or edit its auto-managed region.
